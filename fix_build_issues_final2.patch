--- frontend/src/core/annotations/readers.ts
+++ frontend/src/core/annotations/readers.ts
@@ -26,7 +26,7 @@
   if (typeof annotation.data.fillColor === 'string') return annotation.data.fillColor;
   if (typeof annotation.data.backgroundColor === 'string') return annotation.data.backgroundColor;

-  if (annotation.type === 'rectangle' || annotation.type === 'ellipse') return 'transparent';
+  if (annotation.type === 'shape-rect' || annotation.type === 'shape-ellipse') return 'transparent';
   return fallback;
 }

--- frontend/src/core/search/overlayReplace.ts
+++ frontend/src/core/search/overlayReplace.ts
@@ -29,7 +29,7 @@
           strokeColor: 'transparent',
           strokeWidth: 0,
           textColor: '#000000',
-          textAlign: 'left',
+          textAlign: 'left' as const,
           zIndex: 10,
         },
         createdAt: Date.now(),
--- frontend/src/core/search/indexer.test.ts
+++ frontend/src/core/search/indexer.test.ts
@@ -15,7 +15,7 @@
   };

   it('returns empty array when query is empty', () => {
-    expect(SearchIndexer.search(mockCache, '', {})).toEqual([]);
+    expect(SearchIndexer.search(mockCache, '', { caseSensitive: false, wholeWord: false, useRegex: false })).toEqual([]);
   });

   it('finds simple matches', () => {
