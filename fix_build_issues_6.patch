--- frontend/src/core/macro/executor.ts
+++ frontend/src/core/macro/executor.ts
@@ -53,9 +53,9 @@
           size: step.size ?? 'a4',
           blocks: [
             { type: 'image-header', src: step.imageSrc, height: step.headerHeight ?? 200 },
-            { type: 'heading', text: step.title, level: 1 },
-            ...(step.subtitle ? [{ type: 'heading' as const, text: step.subtitle, level: 3, color: '#64748b' }] : []),
+            { type: 'heading', text: step.title, level: 1 as const },
+            ...(step.subtitle ? [{ type: 'heading' as const, text: step.subtitle, level: 3 as const, color: '#64748b' }] : []),
             { type: 'divider' },
             ...(step.bodyMarkdown ? [{ type: 'rich-text' as const, markdown: step.bodyMarkdown }] : []),
           ],
         });
@@ -375,10 +375,6 @@
         break;
       }

-      case 'add_content_page':
-      case 'add_image_header_page':
-        break; // Handled above
-
       default: {
         // exhaustiveness check
         const _exhaustiveCheck: never = step;
--- frontend/src/core/macro/generationBuiltins.ts
+++ frontend/src/core/macro/generationBuiltins.ts
@@ -28,7 +28,6 @@
   professional_report_full: {
     id: 'professional_report_full',
     name: '✦ Generate Full Report (3 pages)',
-    init: 'new',
     steps: [
       // Page 1: Cover
       {
--- frontend/src/core/macro/types.ts
+++ frontend/src/core/macro/types.ts
@@ -15,11 +15,6 @@

 import type { ContentBlock } from './layout/LayoutEngine';

-export interface InsertPosition {
-  mode: 'start' | 'end' | 'before' | 'after';
-  relativeTo?: number; // 0-based index if before/after
-}
-
 export type MacroStep =
   | {
       op: 'add_content_page';
