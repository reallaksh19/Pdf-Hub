# Cross-agent dependency notes

A1 must land before A2/A6 fully wire actions.
A4 should land before A5 finishes interaction edge cases.
A5 and A7 must coordinate on note/callout/review state, but A7 owns sidebar/review workflow.
A8 starts after A1 contracts are frozen, then hardens all merged work. This matches the attached orchestrated merge model.
