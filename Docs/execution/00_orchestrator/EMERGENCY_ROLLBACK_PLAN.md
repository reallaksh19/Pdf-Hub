# Emergency Rollback Plan

If the final integration (Wave 4) fails global gates:
1. Revert to pre-A1 state.
2. Isolate failing agent branches.
3. Re-integrate passing branches sequentially.
4. Delay failing features to next release cycle.
